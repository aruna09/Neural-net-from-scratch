#include <stdio.h>
#include <conio.h>

//declaring nodes
struct node{
	int weights[100];
	int feature_values[100];
	int bias;
};

//function to init data, i.e features and labels.
void generate_data(){
	//we are gonna have a 10X100 matrix,where each row corresponds 
	//to the laid out pixel vales and the corresponding labels for it.
}
void init_data(){
	//this is where we initialize the weights and the feature_value array;
}
 
void main(){
	generate_data()
	init_data()
}